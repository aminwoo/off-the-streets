# Get this from https://dashboard.stripe.com/apikeys
STRIPE_SECRET_KEY=sk_test_replace_me
# Get this from `stripe listen --forward-to localhost:3000/api/stripe-webhook`.
STRIPE_WEBHOOK_SECRET=whsec_replace_me
# Optional: required when deploying behind a proxy or using a custom domain.
BASE_URL=http://localhost:3000
# Production: point this at a persistent disk mount, e.g. /app/data.
DATA_DIR=data